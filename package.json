{
  "name": "telegraf-session-mongo",
  "version": "1.0.0",
  "description": "MongoDB Session driver for Telegraf",
  "main": "lib/session.js",
  "repository": "git@github.com:ExceedeMedia/telegraf-session-mongo.git",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["telegraf", "telegraf-session", "mongodb", "session", "middleware", "telegram", "telegram-bot"],
  "author": "Vasili Sviridov <vasili@sviridov.ca>",
  "license": "MIT",
  "engines": {
    "node": ">=7.0"
  },
  "files": [
    "lib/session.js"
  ],
  "dependencies": {
    "debug": "^2.6.0"
  },
  "peerDependencies": {
      "telegraf": "^2.0.0 || ^3.0.0"
  }
}
